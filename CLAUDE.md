@AGENTS.md

# Claude Code

`AGENTS.md` is the canonical project contract (shared with Codex and Cursor).

For Payload implementation detail, use `.agents/skills/payload`. For DB/migrate:create behavior, see the Database section in `AGENTS.md` and `.cursor/rules/database-migrations.mdc`.
