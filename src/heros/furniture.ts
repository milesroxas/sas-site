/**
 * The hero's furniture voice: kickers, meta labels and small figures are the
 * same mono capitals, so a hero's margin notes read as one set rather than as
 * a second hierarchy. Stated once: a second treatment in another hero would
 * be drift, not hierarchy.
 */
export const HERO_FURNITURE = 'font-mono text-xs/4 uppercase tracking-widest'
