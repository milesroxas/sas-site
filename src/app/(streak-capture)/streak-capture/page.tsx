import { StreakCapture } from '@/features/immersive'

export const metadata = { robots: { index: false, follow: false }, title: 'Streak Field capture renderer' }
export default function CapturePage() { return <StreakCapture /> }
