/**
 * The contact page that renders at `/contact` itself rather than under it.
 * Shared by the route and the surfaces registry so the URL a sitemap prints
 * and the URL the app serves can never disagree.
 */
export const CONTACT_INDEX_SLUG = 'contact'
