/**
 * `lite-youtube-embed` ships untyped JavaScript with no exports: importing it
 * defines the `lite-youtube` custom element and nothing else. The element's
 * own attributes are typed in `lite-youtube.d.ts`.
 */
declare module 'lite-youtube-embed'
