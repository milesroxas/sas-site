# Animations

The site's motion systems, where each lives, and the single-source-of-truth contract that keeps tuning DRY. WebGL/shader effects — plus the DOM-only `ScrambleText` — have their own doc: [immersive-effects.md](immersive-effects.md).

## The systems

| System | What it does | Source of truth | Demo / tuning surface |
|--------|--------------|-----------------|----------------------|
| Route transitions | View Transition API motion between pages: directional slides, fades, shared-element morphs, hero recede | `src/shared/ui/view-transition/view-transition.css` (`:root` timing vars + recipes); navigation types in `src/shared/lib/view-transition/constants.ts` | `/demo/transitions` — simulator with network throttle |
| Scroll reveal (GSAP) | Section entrances for work-page blocks: `data-reveal` text drops in with a blur settle, `data-reveal="media"` wipes open as a clipped window while its content settles from a zoom; two tracks synced by `mediaOffset`; plays once per page view | `src/shared/ui/scroll-reveal/scroll-reveal.tsx` — `SCROLL_REVEAL_INTRO`, `SCROLL_REVEAL_UNDER_MEDIA`, `SCROLL_REVEAL_TRIGGER_DEFAULTS` | `/demo/transitions` — one section per reveal |
| Block reveal (CSS) | Whole-block fade-up as generic page, lab, and archive blocks enter; optional staggered children | `src/shared/ui/reveal-section/RevealSection.tsx` + the `.reveal-section` / `.reveal-stagger-item` rules in `src/app/(frontend)/globals.css` | — (edit the CSS in place) |
| Filter swap (CSS) | In-place filter changes (InsightsBrowse): outgoing grid fades fast, incoming cards re-stagger (shares the `reveal-stagger-fade` keyframe); chips and topic blurbs collapse via `grid-template-rows` 0fr↔1fr | `.filter-swap` / `.filter-swap-item` rules in `globals.css`; swap flip timing `GRID_EXIT_MS` in `src/sections/InsightsBrowse/index.tsx` | Storybook — `Sections/InsightsBrowse` |
| Testimonials marquee (GSAP) | Endless vertical card lanes at one shared pixel speed, alternating direction, each lane offset by a fraction of its first card | `TESTIMONIALS_MARQUEE_DEFAULTS` in `src/blocks/TestimonialsMarquee/Marquee.client.tsx` | Storybook — `Blocks/TestimonialsMarquee` |
| Immersive effects (WebGL) | Shader-driven effects: text load-ins, refraction, dispersion, floating cards | `*_DEFAULTS` per effect in `src/features/immersive` | `/demo/immersive` (micro interactions); text load-ins on `/demo/transitions` |
| Takeover menu (GSAP + CSS) | The page frame docks into the menu's preview slot (scale + trailing clip mask); across the dock's back half the window cross-fade dissolves into the current page's hero media — cloned from the hero's `data-hero-media` region (every hero in `src/heros/*` marks one) into a layer injected inside the frame, so the animating mask crops the fade; media-less pages keep the scaled page view; hovering a menu link cross-dissolves the window (fast ease-out) to that page's hero media — sourced server-side per link by `getMenuContent`, stacked inside the same layer so the mask still crops it, dissolving back to the resting state when the pointer leaves; the footer bar fades out immediately (page chrome, never part of the dock); menu content staggers in from the dock's halfway point; the Ask transcript swap is a mask that runs *inside* the docked frame (the overlay sits below `FRAME_Z`, so the panel can never paint over the media directly): an opaque popover-colored cover in the hero layer wipes down over the window's media (340ms GSAP clip-path, cropped by the dock's own mask — `CHAT_WIPE_*` consts), then the frame hides in a same-color switch to the always-mounted panel beneath, whose content stages in on a matching 340ms transition delay (`panelContent` in `MenuAsk`); exit restores the frame instantly (still covered, invisible change) and retracts the cover in 200ms, unmasking the media in place; transcript items (messages, Thinking shimmer, errors) enter with a short `tw-animate-css` rise (`transcriptItemEnter` in `src/features/ask/messages.tsx`); below `md` the chat view hands the nav's space to the transcript — nav + CTA fade then release layout (`transition-discrete`), the preview slot drops its 16:9 ratio and flexes to fill | Open/close consts at the top of `src/Header/Menu/index.tsx`; shared primitives (ease, dissolve, dock geometry) in `src/Header/Menu/motion.ts`; the chat swap mask (`CHAT_WIPE_*` in `index.tsx`, content staging `panelContent` in `src/features/ask/MenuAsk.tsx`) | Storybook — `Features/TakeoverMenu` |
| Menu hero handoff (GSAP) | Clicking a menu link whose destination has hero media does not undock: menu content fades out while the docked window holds the destination's media (a fixed "traveler" element mounted with the same `getCardMotion` scale + centered-clip math as the dock); the traveler expands from the slot to FULL SCREEN (uniform scale — pure expansion, no lateral drift) while the route pushes underneath untagged (hard cut — the traveler owns all visible motion) and on commit the frame unfreezes behind it; at full screen the overlay cuts away (fully covered), and after a short hold the traveler collapses onto the new page's measured `data-hero-media` element with clip-path only — one axis per step (horizontal, then vertical), never diagonally — then dissolves into the real hero once it has painted (the dissolve also masks the clip-only landing's crop delta). Every unmet gate (no media, same page, reduced motion, mid-open click, route/hero timeout) falls back to the plain close or a graceful fade | Handoff tunables at the top of `src/Header/Menu/heroHandoff.ts` | — (exercise in the running app) |

Two different components are named `RevealSection`: `src/shared/ui/reveal-section/` is the CSS block reveal above; `src/blocks/case-study/RevealSection.client.tsx` is the work-page shell around GSAP scroll reveal.

## Route transitions

- `DirectionalTransition` wraps every page via `src/app/(frontend)/template.tsx` and maps transition *types* to CSS classes: `nav-forward` and `nav-back` map to classes of the same name; `nav-lateral` maps to `fade-in` / `fade-out`.
- Links tag their navigation: `CMSLink` takes `transitionDirection: 'forward' | 'back' | 'lateral'` (default `lateral`); raw `next/link` takes `transitionTypes={[...forwardNavTransitionTypes]}` from `@/shared/lib/view-transition`.
- Untagged navigations (browser back/forward, revalidations, menu hero-handoff pushes) hard-cut by design — `default: 'none'`. The handoff relies on this: its GSAP traveler owns all visible motion, so a view-transition animation on the same navigation would double-animate.
- All timing lives in the `:root` block of `view-transition.css` (`--vt-duration-*`, `--vt-ease-hero`, `--vt-slide-distance`). The simulator's copy button emits exactly that block.
- Elements that must not cross-fade get their own named group and `animation: none` — the header, the footer, and the persistent WebGL canvas (`.vt-global-canvas`).
- **Work-media takeover** (`work-open`): "View case study" in `IndustryWork` is a plain `next/link` tagged with `workOpenTransitionTypes` — the fades and fallback glide are declared in `view-transition.css`; the only imperative piece is the glide's beat split (below). The root maps `work-open` to dedicated `work-exit`/`work-enter` classes, and the `morph-hero` shared element (`workImageVtName`, block media ↔ case-study hero media) is delayed by `--vt-duration-exit` and fill-held at the spotlight's rect, so it reads as six strict beats — each position-only or size-only, never both: the old page fades out (and stays out — `both` fill), the media glides straight up/down at the spotlight's size until it is vertically centered in the viewport, x untouched (`--vt-duration-hero-center`), expands about its own center until it covers the viewport (`--vt-duration-hero-expand`), holds there full-screen for `--vt-duration-hero-hold` (no page text or content anywhere — the incoming page stays invisible for the whole full-screen moment), then the new page's content fades in as the media glides, still full-size, until it is centered on the hero rect (`work-enter` delays by exit + center + expand + hold; `--vt-duration-hero-move`) — never layered under the full-screen moment — and, as the final beat and the only size change after the expand, the media shrinks in place into the hero's rect (`--vt-duration-hero-grow`). The IndustryWork spotlight requests a full-width image source (`size="100vw"`) so that exact raster stays sharp at full screen. Movement beats ease with `--vt-ease-hero-travel` (strong in-out); the grow keeps `--vt-ease-hero`. There is no visible media swap — both sides render the same asset: the spotlight snapshot stays opaque for the whole flight (its `cover` crop re-flows as the box resizes) and the hero snapshot only fades in over it during the shrink beat, pixel-identical where crops match and hiding the crop/resolution delta inside the size change where they don't (overriding both layers' `animation-name` also removes the UA fade + `plus-lighter` blend). The beat split lives in `work-image-morph.ts` (`onShare` on the IndustryWork side cancels the browser's one-rect group tween and replaces it with four WAAPI beats reading the same `--vt-*` variables); if that bails (no group animation, reduced motion), the CSS single-rect glide over the summed duration remains as fallback. Persistent chrome (header/footer/canvas) keeps its pinned named groups and never participates. Scroll correctness relies on `LenisRouteReset` (`SmoothScrollProvider`) resetting Lenis + scroll in a *layout* effect — inside the view transition's update callback, before the incoming page is snapshotted. Reduced motion, modified clicks, and browsers without view transitions degrade to plain navigation.

## Scroll reveal

- Wrap the section in `ScrollReveal` (from `@/shared/ui/scroll-reveal`) — or `src/blocks/case-study/RevealSection.client.tsx`, a thin wrapper that adds theme, viewport sizing, and the block's `variant` — and mark descendants:
  - `data-reveal` → text track (drop from above + blur settle, staggered in document order)
  - `data-reveal="media"` → media track: the container acts as a clipped window — a top-origin mask wipes down while the first child inside scales down from `mediaScaleFrom` to rest (no fade, no blur — expensive on large media)
- The two tracks run on one timeline. `stagger` spaces consecutive targets within a track; `mediaOffset` shifts the media track against the text track (negative = media leads, `0` = synced, positive = media trails; ignored on shells with no media). Whether elements overlap falls out of those two numbers — offset past the media duration goes fully sequential.
- **One variant per block, everywhere.** A block whose markup carries `data-reveal` markers maps to its reveal variant exactly once, in `src/blocks/shared/reveal-variants.ts`. Every renderer — Pages/Home (`RenderBlocks`), work (`RenderCaseStudyBlocks`), lab (`RenderLabBlocks`) — reads that map, so the same CMS block plays the same GSAP reveal on every surface; marker-less blocks take the CSS block reveal from their renderer instead. `'self'` blocks (statement links) mount their own shell and must never be wrapped in a second entrance — which is why the post body rich text (where statement links can be embedded) has no CSS reveal wrap.
- **Two reveals, each owned whole.** `SCROLL_REVEAL_INTRO` is the complete reveal for introduction / text-only blocks; `SCROLL_REVEAL_UNDER_MEDIA` is the complete reveal for copy paired with media (its own text tuning + the wipe + the sync offset). They share nothing but the trigger gate, so tuning one never moves the other. Blocks pick one with `variant="intro" | "underMedia"` via `RevealSection`; explicit props still win over the variant's values.
- One timeline and one viewport gate per shell; the entrance is **play-once** — the gate disconnects after it fires, so scrolling back past a revealed section never reverses or replays it (`SCROLL_REVEAL_EXIT_TIME_SCALE` now only speeds up click-driven swap exits). Full-viewport shells (`fullViewportSectionClassName` surfaces) raise the enter gate to `SCROLL_REVEAL_FULLSCREEN_ENTER_THRESHOLD` so the entrance lands while the band owns the screen; shorter shells keep `SCROLL_REVEAL_TRIGGER_DEFAULTS`. Reduced motion renders the final state; server-rendered children stay visible without JS.
- Never hand-roll a new reveal tween. If a shell needs different choreography (see `src/sections/WorkIntro/Section.client.tsx`), it still imports the shared gate (`SCROLL_REVEAL_TRIGGER_DEFAULTS`) and any value one of the reveals already owns (ease, durations, the intro stagger) instead of restating it.
- Audience tabs is a `'self'` block with a bespoke shell (`src/blocks/AudienceTabs/use-audience-tabs-motion.ts`): a phased entrance (heading → tab buttons → list items → media wipe last, media start computed from item count) and a swap whose text transition and image change start on the same click — the incoming image immediately wipes down over the outgoing one on an underlay (the outgoing image never animates, so the container background never shows) while the outgoing text fades out in place and the incoming text snaps in with a fast fade. Every shared value is imported from the reveals; only entrance phase offsets are owned locally. Its entrance targets overlap its swap targets — safe because the entrance is play-once and finishes before any swap can start.
- Dropdown-driven panel swaps (dynamic audience, industry work) use `useRevealSwap` from the same module: `data-swap="text"` / `data-swap="media"` targets fade in place (opacity only — no blur, no lift), then the incoming half fades in. Timings live in `SCROLL_REVEAL_SWAP` (under 250ms); the scroll entrance (`SCROLL_REVEAL_UNDER_MEDIA`) is never replayed. Industry work additionally crossfades media over the outgoing frame at click so the spotlight never empties, and keeps the WebGL canvas mounted (hidden only for the dissolve). Swap targets stay distinct nodes from the shell's `data-reveal` targets. The shared headline-with-dropdown line itself is `src/blocks/shared/heading-dropdown.tsx`.

## The tuning workflow

1. Dial values in on the demo page — every parameter is wired to the section's GUI, with a replay button.
2. **Copy** emits code for the paste target shown in the guide (a `:root` CSS block, a `*_DEFAULTS` const, or component props).
3. Paste at the target. Every consumer reads from there — no call site restates a tuned value.

The two scroll-reveal demo sections map 1:1 onto the two reveal consts — every value of a reveal is controlled in its section, and **Copy** emits the whole const, so a paste can never drop a key. Each section also plots a to-scale timeline diagram of every target's start, with an overlap/sequential readout. The trigger gate (`SCROLL_REVEAL_TRIGGER_DEFAULTS`) is live-tunable in both but not part of either copy snippet — transcribe it manually if you change it.

## Shared foundations

- **Smooth scroll.** Lenis runs site-wide in `root` mode from `SmoothScrollProvider` (`src/providers/`), advanced on the tempus clock so it shares RAF timing with WebGL. It persists across navigations, so `LenisRouteReset` returns it to the top (or the anchor) on every route change. Reduced motion skips the provider entirely and gets native scrolling.
- **Reduced motion.** Every JS system reads `usePrefersReducedMotion` (`src/hooks/`); CSS systems gate on `prefers-reduced-motion`.
- **`scroll-fade` utilities** (`globals.css`) mask the edges of a scroll container using `animation-timeline: scroll()`, with a static-mask fallback. Size via `--scroll-fade-size`; no JS involved.

## Rules

- Every tunable number exists in exactly one place; consumers import, never restate (same contract as [immersive-effects.md](immersive-effects.md)).
- Reuse an existing system before adding a new one; a second usage of a custom tuning gets promoted to the shared source, not copied.
- `prefers-reduced-motion` collapses every system to its final state — any new motion must do the same.
- Demo playgrounds (`src/widgets/transition-demo/`, `src/widgets/immersive-demo/`) are demo-only, never shipped UI.
